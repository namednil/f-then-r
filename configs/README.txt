What are the hyperparameters from the paper called in the config files?

d   maximum_fertility

\lambda_1   length_loss_scheduler.constant
\chi    alignment_confidence_thresh
\lambda_2    alignment_loss_weight
m    pretrain_epochs

\tau    fertility_model.temperature (if not present, it's the default value of 1)




